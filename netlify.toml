[build]
  command = "pnpm build"
  functions = "netlify/functions"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "astro dev"
  port = 8889
  functions = "netlify/functions"
  targetPort = 4321